[package]
name = "check-file"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
html-conform = { path = "../.." }
serde_json = "1"

# Eigener Workspace: dieses Paket gehoert nicht zum Workspace des Monorepos
# (dort unter workspace.exclude) und braucht die Zeile, damit cargo es
# eigenstaendig baut.
[workspace]
