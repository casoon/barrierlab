[package]
name = "web-checks"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
categories.workspace = true
description = "Shared evaluation for web audits: robots.txt grammar, bot classification and longest-match path rules. Computes, does not fetch."
keywords = ["robots-txt", "seo", "audit", "crawler", "ai-crawler"]
readme = "README.md"

[dependencies]
serde = { workspace = true }
