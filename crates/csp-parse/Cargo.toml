[package]
name = "csp-parse"
version = "0.1.0"
edition = "2024"
description = "A parser for the Content Security Policy (CSP) directive grammar"
license = "MIT"
repository.workspace = true

[dependencies]
